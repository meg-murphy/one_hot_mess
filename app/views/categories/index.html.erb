<% @categories.each do |category| %>
  <h1> <%= link_to category.name, "/categories/#{category.id}"%></h1>
  

    <% category.recipes.each do |recipe| %>
      <%= link_to recipe.name, "/recipes/#{recipe.id}" %>

    <% end %>
<% end %>
